# include "../include/User.hpp"
# include "../include/Server.hpp"
# include "../include/Commands.hpp"

void parseCommand(std::string CommandStr)
{
    for(int found = co; i < CommandStr.length; i++)
    {
        if (CommandStr.)
    }
}